import { IconNameType } from "../../../assets/Icon";
import { THEMESTATUS } from "../../layout/Layout";

type HandleOnClickType = (event: React.MouseEvent<HTMLInputElement>) => void;
type HandleOnChangeType = (event: React.ChangeEvent<HTMLInputElement>) => void;

// icon?: IconNameType;
// label?: string;
// status?: THEMESTATUS;
// message?: string;
// onClick?: HandleOnClickType;
// onChange?: HandleOnChangeType;
// placehold?: string;
